name: Upper Landing Bead Gate

on:
  pull_request:
    paths:
      - 'projects/Dubai-Luxury-Villa-AI/web-viewer/app/src/tourData.js'
      - 'projects/Dubai-Luxury-Villa-AI/web-viewer/app/src/walkthroughEngine.js'
      - 'projects/Dubai-Luxury-Villa-AI/web-viewer/app/src/VillaViewer.jsx'
      - 'projects/Dubai-Luxury-Villa-AI/web-viewer/app/qa/upper-landing-bead.mjs'
      - 'projects/Dubai-Luxury-Villa-AI/validation/upper-landing-bead-trajectory-v1.md'
      - '.github/workflows/upper-landing-bead-gate.yml'
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: upper-landing-bead-${{ github.event.pull_request.number || github.ref }}
  cancel-in-progress: true

jobs:
  upper-landing-bead:
    runs-on: ubuntu-24.04
    defaults:
      run:
        working-directory: projects/Dubai-Luxury-Villa-AI/web-viewer/app

    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: '22'

      - name: Install app dependencies
        run: npm install --no-audit --no-fund

      - name: Build viewer
        run: npm run build

      - name: Install Playwright Chromium
        run: |
          npm install --no-save playwright@1.55.0
          npx playwright install --with-deps chromium

      - name: Start local production preview
        run: |
          npm run preview -- --host 127.0.0.1 --port 4173 > /tmp/villa-upper-landing.log 2>&1 &
          for i in {1..60}; do
            if curl -fsS http://127.0.0.1:4173/ >/dev/null; then
              exit 0
            fi
            sleep 1
          done
          cat /tmp/villa-upper-landing.log
          exit 1

      - name: Capture Upper Landing bead
        env:
          VILLA_URL: http://127.0.0.1:4173/
          QA_OUTPUT: qa-upper-landing-bead-output
        run: node qa/upper-landing-bead.mjs

      - name: Upload Upper Landing bead evidence
        if: always()
        uses: actions/upload-artifact@v4
        with:
          name: dubai-villa-upper-landing-bead
          path: projects/Dubai-Luxury-Villa-AI/web-viewer/app/qa-upper-landing-bead-output
          if-no-files-found: warn
          retention-days: 14
